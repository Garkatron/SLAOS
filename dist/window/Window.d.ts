import { ChannelsMap } from "../glue/types";
import Canvas from "./Canvas";
import { WindowHandle, WindowCallbacks, WindowControls, WindowInstanceId, WindowProps, WindowVisibility } from "./types";
export declare const WINDOW_DEFAULT_PROPS: WindowProps;
export declare class WindowInstance<P extends WindowProps> {
    private instanceId;
    private frame;
    private content;
    windowProps: P;
    private canvas;
    private visibility;
    private sizeState;
    private open;
    private callbacks;
    private unsub?;
    private controls;
    private onCloseClick;
    private onHidden;
    private onMaximizeClick;
    private restoreState;
    id(): WindowInstanceId;
    constructor(instanceId: WindowInstanceId, frame: HTMLElement, content: HTMLElement, windowProps: P, canvas: Canvas<any, any, any>);
    api(): WindowHandle;
    getChannels<C extends ChannelsMap>(): C;
    setCallbacks(callbacks: WindowCallbacks): void;
    toggleMaxMinSize(): void;
    private maximize;
    private minimize;
    private restore;
    close(): void;
    openWindow(): void;
    setSize(width: number, height: number): void;
    setPosition(x: number, y: number): void;
    toggleVisibility(): void;
    setVisibility(visibility: WindowVisibility): void;
    setTitle(title: string): void;
    setContent(content: HTMLElement): void;
    setControls(controls: WindowControls): void;
    detachControls(): void;
    detachSignal(): void;
    render(parent: HTMLElement | undefined, state: any): void;
    isOpen(): boolean;
    isVisible(): boolean;
}
//# sourceMappingURL=Window.d.ts.map